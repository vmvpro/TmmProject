<Window x:Class="TmmProjectWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TmmProjectWPF.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Button Content="Show Window Student" HorizontalAlignment="Left" 
                Height="62" Margin="55,53,0,0" VerticalAlignment="Top" Width="127"
                 Command="{Binding ShowStudentCommand}">
        </Button>

        <Button Content="Show SelectionWork" HorizontalAlignment="Left" 
        	Height="62" Margin="55,157,0,0" VerticalAlignment="Top" Width="127"
        	Command="{Binding ShowSelectionWorkCommand}"/>
        
        <Button Content="Test" HorizontalAlignment="Left" Height="62" Margin="306,53,0,0" VerticalAlignment="Top" Width="154"
                Command="{Binding TestViewShowCommand }"/>
        
        

    </Grid>
</Window>
